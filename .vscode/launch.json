{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug current task",
            "type": "cppdbg",
            "request": "launch",
            "program": "${fileDirname}/${workspaceFolderBasename}",
            "cwd": "${fileDirname}",
            "MIMode": "gdb",
            "preLaunchTask": "Build current task"
        }
    ]
}